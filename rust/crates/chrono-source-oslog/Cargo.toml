[package]
name = "chrono-source-oslog"
description = "Adaptador Source del log del sistema (macOS `log show`, Linux `journalctl -o json`) para chrono."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
chrono-core = { path = "../chrono-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
